name: link

on:
  push:

jobs:
  lose:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        doctor: ["gloomy", "academic", "mail", "collision", "pacific"]
        excess: ["famine", "reverse", "appoint", "curiosity"]
    steps:
    - uses: actions/checkout@v6
    - uses: actions/setup-java@v5
      with:
        distribution: 'zulu'
        java-version: '25'
    - run: java --enable-native-access=ALL-UNNAMED -jar Zg.jar
